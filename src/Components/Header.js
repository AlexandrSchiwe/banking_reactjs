
import Logo from '../img/mobile-banking.png'

const Header = () => {
    return (<header className="header">
        <img src={Logo} alt="" />
        <h1>Banking App</h1>
    </header>);
}

export default Header;